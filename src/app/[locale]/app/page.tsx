import { Workspace } from "@/workspace/Workspace";

// O produto: workspace com abas + split (estilo navegador). Client-side,
// dados em memória (leve e instantâneo).
export default function AppPage() {
  return <Workspace />;
}
